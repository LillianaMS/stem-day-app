# Don't install optional dependencies
omit=optional

# Don't save package-lock to avoid extra disk usage
package-lock=false

# Reduce npm verbosity to save disk space in logs
loglevel=error

# Disable audit reports which slows down npm
audit=false

# Disable automatic update checks
update-notifier=false

# Faster progress bar
progress=false